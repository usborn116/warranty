<p style="color: green"><%= notice %></p>

<h1>Items</h1>
<%= render 'search' %>

<div id="items">
  <% @items.each do |item| %>
    <%= render item %>
    <p>
      
    </p>
  <% end %>
</div>

<%= link_to "New item", new_item_path %>
